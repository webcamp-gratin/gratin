<div class="container">
  <div class="row">
    <div class="col-auto mt-3">
      <h4 class="table-active">ジャンルの一覧・追加</h4></br>
    </div>
  </div>
  <%= render 'layouts/error', model: @genre %>
    <%= form_with model: @genre, url: admin_genres_path, method: :post, local:true do |f| %>
      <div class="form-inline col-8">
        <%= f.label :ジャンル名, class: "col-3 font-weight-bold" %>
        <%= f.text_field :name, :placeholder => "ジャンル名",  class: "form-control col-5 mr-3" %>
        <%= f.submit "新規登録", class: "btn btn-success btn-lg col-3 ml-4" %>
      </div>
    <% end %>
    </br>
    <div class="row">
      <table class="table col-4">
        <thead>
          <tr class="table-active">
            <th style="width: 60%">ジャンル</th>
            <th style="width: 40%"></th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td class="text-left">
                <%= genre.name %>
              </td>
              <td>
                <%= link_to "編集する", edit_admin_genre_path(genre), class: "btn btn-success ml-3" %>
              </td>
            </tr>
          <% end %>
        </tbody>
     </table>
    </div>
</div>